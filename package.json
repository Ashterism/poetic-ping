{
  "name": "poetic-ping",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "build": "npm run build --workspace @poetic-ping/web && npm run build --workspace @poetic-ping/worker",
    "build:worker": "npm run build --workspace @poetic-ping/worker",
    "deploy:worker": "npm run deploy --workspace @poetic-ping/worker",
    "build:web": "npm run build --workspace @poetic-ping/web",
    "dev:web": "npm run dev --workspace @poetic-ping/web",
    "dev:worker": "npm run dev --workspace @poetic-ping/worker",
    "test": "npm run test --workspace @poetic-ping/worker",
    "typecheck": "npm run typecheck --workspaces --if-present"
  },
  "engines": {
    "node": ">=22"
  },
  "overrides": {
    "workerd": "1.20260815.1"
  },
  "allowScripts": {}
}
